{
  "name": "@agent-engine/tool-karos-media",
  "private": true,
  "version": "0.0.1",
  "description": "Real image sourcing (search + download) for agents that need pictures.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "pretest": "node ../../../scripts/check-dist-freshness.mjs",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/telemetry": "*",
    "@agent-engine/tool-common": "^0.0.1",
    "@agent-engine/tool-karos-scraper": "^0.0.1",
    "@google/genai": "^2.18.0",
    "playwright": "1.53.0",
    "zod": "^4.4.3"
  }
}
